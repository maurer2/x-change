import React from 'react';
import PropTypes from 'prop-types';

const Masthead = () => {
  const name = 'Masthead';

  return (
    <div>
      {name}
    </div>
  );
};

Masthead.propTypes = {
};

export default Masthead;
