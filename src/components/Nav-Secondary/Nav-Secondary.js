import React from 'react';
import PropTypes from 'prop-types';

const NavSecondary = () => {
  const name = 'NavSecondary';

  return (
    <div>
      {name}
    </div>
  );
};

NavSecondary.propTypes = {
};

export default NavSecondary;
