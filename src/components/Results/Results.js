import React from 'react';
import PropTypes from 'prop-types';

const Results = () => {
  const name = 'Results';

  return (
    <div>
      {name}
    </div>
  );
};

Results.propTypes = {
};

export default Results;
