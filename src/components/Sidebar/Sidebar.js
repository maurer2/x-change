import React from 'react';
import PropTypes from 'prop-types';

const Sidebar = () => {
  const name = 'Sidebar';

  return (
    <div>
      {name}
    </div>
  );
};

Sidebar.propTypes = {
};

export default Sidebar;
