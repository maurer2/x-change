import React from 'react';
import PropTypes from 'prop-types';

const UserCard = () => {
  const name = 'UserCard';

  return (
    <div>
      {name}
    </div>
  );
};

UserCard.propTypes = {
};

export default UserCard;
