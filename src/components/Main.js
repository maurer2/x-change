import React from 'react';
import PropTypes from 'prop-types';

const Main = () => {
  const name = 'Main';

  return (
    <div>
      {name}
    </div>
  );
};

Main.propTypes = {
};

export default Main;
