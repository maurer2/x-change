import React from 'react';
import PropTypes from 'prop-types';

const Filters = () => {
  const name = 'Filters';

  return (
    <div>
      {name}
    </div>
  );
};

Filters.propTypes = {
};

export default Filters;
